spring:
  datasource:
    # 请根据实际情况修改用户名和密码
    url: jdbc:mysql://localhost:3306/pcdiy_evaluator?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: HANS040912
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  # 打印 SQL 语句，方便我们后续调试规则引擎的查询
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl